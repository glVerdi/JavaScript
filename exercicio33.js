/*
Escreva uma função chamada `calculaIMC` que receba dois parametros: `altura` e `peso` e retorne o resultado do calculo de IMC
*/
function calculaIMC(altura, peso) {
    return peso / (altura ** 2); 
}