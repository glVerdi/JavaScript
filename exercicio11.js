function exponencial(base, expoente) {

/*
Declare uma variável chamada `potencia` e atribua a ela o valor da variável 'base' elevada a variável expoente.
*/
      
var potencia = base ** espoente;        

 return potencia;
}